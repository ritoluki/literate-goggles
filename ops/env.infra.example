# Local-only configuration; not production credentials.
# Agent generates random DB secrets into ignored local env (never commit output).
POSTGRES_DB=ban_gon_demo
POSTGRES_USER=ban_gon_local
POSTGRES_PASSWORD=
POSTGRES_PORT=5432
REDIS_PORT=6379
MAIL_SMTP_PORT=1025
MAIL_UI_PORT=8025
# Generate compose with explicit 127.0.0.1 port bindings.
# Infrastructure image tags/digests must be pinned in infra/ and VERSIONS.
